# PERFORMANCE — 大量出力スループット実測（#16）

計測日: 2026-07-03 / 機材: i7-1195G7・16GB（メイン開発機）/ 対象: v1.3.3 vs v1.1.1（公式リリースバイナリ）

## 方法論

- **分離プロファイル**で計測用インスタンスを起動（`XDG_CONFIG_HOME`＋`WEBVIEW2_USER_DATA_FOLDER` を一時ディレクトリへ）。実運用インスタンス・設定・セッションに一切影響しない。
- 負荷は**クリップボード貼り付け（IME 非経由）**で実ペインに注入し、実際の入出力経路（ConPTY→Rust reader→IPC Channel→xterm パース→描画）を通す。
- **2指標**:
  - `producer_ms` — 注入コマンド自身が計る `Stopwatch`（`cmd /c type <file>` の完了まで）＝ ConPTY 取り込み側のスループット。
  - `quiesce_ms` — 注入 Enter からプロセス群（orb.exe＋当該 WebView2 子プロセス）の CPU が静穏化するまで＝**描画パイプライン完走の代理指標**（フロントのパース残債を含む）。
- 負荷: ①洪水 6.4MB（200,000行×30字）②疑似ビルドログ 3.3MB（30,000行・可変幅・ANSI色付き）。洪水は各3回で中央値。
- ハーネス: 一時ディレクトリの `perf-run.ps1`（クリック実フォーカス→貼り付け→Enter→250ms間隔サンプリング）。再計測時は同等スクリプトを再作成すればよい（本ファイルの方法論が仕様）。

## 結果

### 洪水 6.4MB（中央値/3回）

| 構成 | producer | quiesce（e2e代理） | v1.1.1比 | CPU消費 | ピークWS |
|---|---:|---:|---:|---:|---:|
| v1.1.1（基準） | 10.3s | **11.3s** | — | ~38 core·s | 0.6-0.7GB |
| v1.3.3 素（WebGL） | 10.1s | **11.3s** | **±0%** | ~38 core·s | 0.6-0.8GB |
| v1.3.3 背景ON（DOM） | 10.3s | **13.1s** | **+15%** | ~40 core·s | 0.7-0.9GB |

### 疑似ビルドログ 3.3MB（ANSI付き・各1回）

| 構成 | producer | quiesce |
|---|---:|---:|
| v1.1.1 | 4.5-4.8s | 5.2-5.9s |
| v1.3.3 素 | 4.8s | 7.6s ※ |
| v1.3.3 背景ON | 4.8s | 5.9s |

※ 単発サンプルで静穏判定はバックグラウンドタイマー（usage 30s ポーリング等）の CPU ブリップに ±1-2s 感度がある。producer が3構成同値のため、この差はノイズと判断（背景ONの方が速く出ていることからも系統差ではない）。

## 結論（#16 の合格ライン判定）

1. **v1.1.1 → v1.3.3 の回帰: ゼロ**（洪水 e2e ±0%・±20% ゲート PASS）。v1.2〜v1.3 で足した ブロック耐久ログ（#31）・E/C マーカー解析（#33）・装飾/ツールバーは、**コマンド毎 O(1)** の設計どおり大量出力のホットパスに乗っていない。
2. **背景画像 ON（＝DOM レンダラ）は洪水時 +15%**。opt-in の贅沢として妥当な範囲（既定 OFF 構成には影響ゼロ）。
3. パイプラインは **ConPTY 律速**（e2e の ~90% が取り込み側）。フロント側のパース残債は洪水 6.4MB でも +1.0〜1.2s。今後 xterm/描画側を最適化しても伸び代は残り ~10%、劇的改善を狙うなら ConPTY 側（チャンクサイズ・スループット）が対象。
4. 計測中もウィンドウはハングせず（静穏検出ループが 120s デッドラインに達した有効試行なし）。

## 既知の注意

- v1.1.1 は起動スプラッシュ＋起動直後打鍵ドロップ（#39 未修正時代）があり、コールドプロファイル初回注入が失敗する。計測はウォーム状態で行うこと。
- `quiesce_ms` は代理指標（CPU静穏化）。ブロックの JSONL `ended_at`（D マーカーのパース完了時刻）でも e2e を裏取りできる（v1.2.0+）。
